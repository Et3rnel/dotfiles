[alias]
	r = !git checkout main && git pull origin main && git checkout - && git rebase main
	force = push --force
	trigger-ci = !git commit --amend --no-edit && git push --force-with-lease
[user]
	name = Eth3r
	email = pasquiong@gmail.com
[credential]
	helper = osxkeychain
[core]
	excludesfile = ~/.dotfiles/git/.gitignore.dist
[push]
	autoSetupRemote = true
